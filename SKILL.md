---
name: gpt-image-2-prompter
description: 为 GPT-image-2 和 ChatGPT 图像 2.0 设计、改写、诊断和迭代出图提示词。Use when 用户需要把粗略想法、参考图、产品/品牌/课程/桌游视觉需求、营销图、信息图、角色设定、样卡板、图片编辑、多图合成、PSD 分层拆分、素材分拆或矢量友好需求，转成可直接用于 GPT-image-2 的结构化提示词、参数建议、迭代策略或提示词库。
---

# GPT-image-2 提示词设计

## 工作原则

先把用户需求转成可验证的出图目标，再写提示词。默认输出中文说明 + 可直接复制的中文 GPT-image-2 提示词；如果用户明确要 API、英文或批量模板，再切换格式。

优先用 GPT-image-2 生成真实视觉样卡、概念图、产品图、透明素材和结构化视觉成品；不要只用 CSS/SVG 示意替代用户要看的视觉效果。正式长中文文案、复杂排版和可变内容优先保留在 HTML/PPT/设计稿等可编辑层；但当用户明确要交付一张最终海报、信息图、图解或幻灯片图片时，可把已确认的短文案、数字和标签直接写进提示词，并用更高质量出图后逐项校验。

## 先判定任务类型

1. **从零生成**：海报、样卡、视觉提案、角色、产品图、课程配图、信息图。
2. **编辑图片**：替换局部、改光线/天气/材质、翻译图中文字、产品抠图与样机。
3. **多图合成**：产品图 + 风格参考、人物/角色 + 场景、品牌资产 + 版式参考。
4. **提示词诊断**：图像跑偏、文字不准、主体变形、风格过重、构图不稳、复杂度过高。
5. **PSD/分层拆分友好**：用户提到 PSD、图层、分拆、素材拆分、矢量、可编辑、后期使用或导出组件时，生成“适合后期拆层”的设计稿提示词和图层清单。
6. **透明素材**：用户要透明底 logo、商品抠图、贴纸、图标、角色或演示素材时，生成透明背景 PNG/WebP 的参数建议与隔离主体提示词；不要把透明输出误承诺为 PSD 图层。

缺少关键信息时只问最少问题：用途、比例/尺寸、主体、风格边界、必须保留/禁止内容。若可以合理假设，直接给默认方案并标明假设。

## 输出结构

每次产出提示词时，按这个顺序给结果：

1. **推荐参数**：`model="gpt-image-2"`；`size`；`quality`；按需给出 `background`、`output_format`、`output_compression`。普通成图不必显式设置背景；透明素材固定说明 `background="transparent"` + PNG/WebP，且 PNG 不传 `output_compression`。
2. **GPT-image-2 规则项**：先列出本次要写进提示词的模型特性规则，例如文字保真、结构化视觉、身份/几何保留、编辑保真、材质真实感、多图引用、尺寸边界。
3. **质感控制**：当任务需要干净、商业级、插图、Logo、产品图、样卡、课程图或用户提到“去 AI 感”时，加入去 AI 感质感控制块。
4. **最终提示词**：用短标题分段，顺序为目标/用途 -> 画面主体 -> 场景与构图 -> 风格与材质 -> 质感控制 -> GPT-image-2 rules -> 文字要求 -> 保留项/禁止项。
5. **检查点**：列出 3-6 个出图后要核对的点。
6. **迭代指令**：给 1-3 条单点 follow-up，不要一次塞入大段新要求。

`GPT-image-2 规则项` 不是外部解释，必须转写进最终提示词。不要只给通用审美词。

## 中文输出契约

默认用中文交付全部内容，包括推荐参数说明、规则项解释、最终提示词、检查点和迭代指令。最终提示词里的字段名也用中文，例如“目标”“画面主体”“场景与构图”“风格与材质”“文字要求”“限制条件”。

只保留必要英文：

- API 参数名：`model`、`size`、`quality`、`background`、`output_format`、`output_compression`。
- 固定硬约束段落名：`GPT-image-2 rules:`，用于确保提示词里显式带上模型特性规则；该段落下的规则内容默认写中文。
- 文字保真控制词：`EXACT, verbatim, no extra characters`。
- 图片索引：`Image 1`、`Image 2`、`Image 3`。
- 用户明确要求英文提示词、英文素材或双语输出时，再输出英文或双语。

不要因为参考模板里出现英文控制词，就把整段提示词写成英文。

## 输出前硬性自检

交付前逐项检查；任一不满足就重写提示词：

- 输出说明、最终提示词主体、检查点和迭代指令是否为中文。
- 是否只保留了必要英文控制词，而不是整段英文模板。
- 最终提示词内是否有 `GPT-image-2 rules:` 段落。
- `GPT-image-2 rules:` 是否包含本任务相关的模型特性，而不是泛泛写“高质量、精美、专业”。
- 如果有图片内文字，是否写明精确文案、只出现一次、可读、无额外字符。
- 如果是编辑任务，是否写明只改变什么、必须保留什么。
- 如果是多图输入，是否按 `Image 1/2/3` 说明每张图的用途。
- 如果需要透明底，是否同时写明透明背景、PNG/WebP 输出、无布景/实底/棋盘格/粘连阴影，并保留 alpha 通道。
- 是否避免为 `gpt-image-2` 推荐或传入 `input_fidelity`；该模型默认高保真，此参数不生效。
- 如果是卡牌、包装、海报、PPT 或印刷图，是否写明比例、留白、安全区或可编辑文字区。
- 如果用户要求“去 AI 感”、干净商业质感、精致插图或产品级视觉，是否加入了质感控制块。
- 如果用户要求 PSD、分层、分拆、素材拆分、透明底或矢量友好，是否说明 GPT-image-2 生成 PNG/JPEG/WebP 视觉稿，不承诺直接输出 PSD，并给出图层/素材拆分清单。
- 是否给出出图后检查点和单点迭代指令。

不合格输出示例：只写“高级、电影感、精致、真实、留白丰富”。这些词可以作为风格补充，但不能替代 GPT-image-2 的规则项。

## GPT-image-2 参数规则

- 默认模型用 `gpt-image-2`。
- 常规预览和大量探索先用 `quality="low"`；客户可见资产、密集文字、近景人物、身份保留、复杂信息图、高分辨率输出优先比较 `medium` 或 `high`。
- 常用尺寸：`1024x1024` 方图，`1536x1024` 横图，`1024x1536` 竖图，`2560x1440` 16:9 高质量上限参考。
- 自定义尺寸要同时满足：长边小于 `3840px`，两边都是 `16` 的倍数，长短边比例不超过 `3:1`，总像素在 `655360` 到 `8294400` 之间。
- 超过 `2560x1440` 的输出要提醒结果更不稳定，必要时先生成 2K 再后处理放大。
- `gpt-image-2` 不使用 `input_fidelity`；人物、产品、角色和多图编辑要通过清楚的保留项、输入图索引和单点迭代锁定高保真结果。
- 透明素材用 `background="transparent"`，并选 `output_format="png"` 或 `"webp"`；PNG 不传 `output_compression`，JPEG 不支持透明。提示词必须要求“主体独立、完全透明背景、不要场景/实底/棋盘格/多余阴影”。

## 提示词写法

使用可扫描的分段，不追求复杂语法。每个提示词必须明确：

- **用途**：广告概念、课程封面、桌游样卡、产品目录图、信息图、社媒图等。
- **主体**：谁/什么物体，关键特征，数量，位置关系。
- **构图**：横竖比例、镜头远近、视角、留白、焦点、前中后景。
- **质感**：摄影/插画/3D/纸张/织物/金属/印刷/手绘等真实材料线索。
- **光线与颜色**：日光、棚拍、低饱和、强对比、品牌色、背景色。
- **约束**：要保留什么、只改变什么、不要水印/多余文字/商标/额外 logo。

需要真实感时，直接使用 `photorealistic`、真实纹理、自然瑕疵、真实光影和摄影构图描述。不要堆精确相机参数，除非它们只是辅助风格方向。

## GPT-image-2 特性规则项

根据任务选择 3-8 条写入最终提示词的 `GPT-image-2 rules` 段落。优先选择和本次任务直接相关的规则，不要机械全塞。

- **生产级真实感**：要求真实材质、自然光照、接触阴影、边缘质量、色彩层次和可交付构图。
- **结构化视觉**：信息图、教学图、多面板、样卡板、角色设定和设计板要明确网格、层级、分区、阅读顺序和留白。
- **成品式图解**：用户明确要图片本身承担海报、幻灯片、流程图或图表时，按成品规格写 canvas、层级、真实数据/标签、阅读距离和视觉语言；小字、图例、坐标轴或脚注用 `quality="high"`，出图后逐项核对文字与数据。
- **文字保真**：图片内文字必须短；精确文案放引号，要求 `EXACT, verbatim, no extra characters`，指定只出现一次和可读性。
- **身份/角色一致性**：人物、角色、产品或品牌资产要锁定脸部/轮廓/服装/比例/几何/标签，不让风格化覆盖识别特征。
- **图片编辑保真**：局部编辑必须写 `Change ONLY...` 和 `Keep everything else unchanged`，并列出相机角度、光照、阴影、布局和周围物体。
- **多图输入索引**：多参考图必须写 `Image 1/2/3` 的角色，以及从哪张图取主体、风格、材质、构图或品牌色。
- **产品/包装清晰度**：商品图要保留几何和标签可读性，要求干净边缘、无光晕、轻微抛光、真实接触阴影。
- **去 AI 感质感控制**：干净商业视觉、插图、Logo、产品图、样卡和课程图要控制高光、阴影、边缘、渐变、细节密度和色彩纯净度，避免油腻、塑料、过锐和脏噪。
- **PSD/分层拆分友好**：不要承诺直接输出 PSD；要求画面按背景、主体、装饰、文字占位、按钮/标签、阴影高光、图标等独立素材组构成，并输出图层命名、透明 PNG 导出和矢量重绘建议。
- **透明资产**：需要复用的单个 logo、商品、贴纸、图标或角色，明确“完全透明背景、主体独立、无背景景物、无棋盘格、无粘连阴影”，并将输出锁定为 PNG/WebP；透明图像的 alpha 通道必须在下游保留。
- **灵活比例和印刷意识**：海报、卡牌、书签、包装、PPT 图要写明成品比例、出血/安全边距/广告留白/可编辑文字区。
- **迭代友好**：避免一次性塞太多风格词；提示词应能通过单点 follow-up 调整光线、复杂度、文字和构图。

推荐在最终提示词中使用这个段落名：

```text
GPT-image-2 rules:
- [Select task-specific model rule 1.]
- [Select task-specific model rule 2.]
- [Select task-specific model rule 3.]
```

## 去 AI 感质感控制块

当用户要求“去 AI 感”、画面更自然、更干净、更高级，或任务是插图、Logo、产品图、样卡、课程图、品牌视觉时，默认把下面的短块写入最终提示词。若用户明确要纪实粗粝、胶片颗粒、脏旧纹理、复古印刷或手工瑕疵，不要机械加入“无颗粒、无瑕疵”。

```text
质感控制：
自然干净的画面，柔和漫射光，真实平滑的阴影，清晰但不过度锐化的边缘，干净色彩过渡，平衡对比度，细节克制有层次。避免油腻高光、塑料质感、噪点颗粒、脏污纹理、浑浊色彩、斑驳阴影、混乱细节和过度渲染。
```

需要更强约束时，在 `GPT-image-2 rules:` 中加入：

```text
- 控制典型 AI 感：不要油腻高光、塑料质感、过度锐化、过强对比、斑驳阴影、脏污噪点、浑浊色彩或混乱细节。
- 光影和材质要自然可信，边缘清晰但不要硬到像抠图，色彩过渡干净柔和。
```

## PSD/分层拆分友好规则

GPT-image-2 不要被描述成能直接输出 PSD 分层源文件。它适合生成 PNG/JPEG/WebP 视觉稿，或生成适合后期拆层的素材图。用户要求 PSD、分层、分拆、素材拆分、透明底、矢量、可编辑、后期使用时，按这个方式交付：

1. 先说明边界：生成“PSD 拆层友好的设计稿/素材拆分提示词”，真正 PSD 需要 Photoshop/Figma/人工或脚本后期制作。
2. 提示词里要求元素彼此不重叠或少重叠，边缘清晰，背景干净，留出拆分空间。
3. 把图层组写清楚：背景、主视觉、产品/角色、装饰元素、图标、标签、按钮/文字占位、阴影高光、纹理。
4. 对每个图层组给后期导出建议：透明 PNG、SVG/矢量重绘、可编辑文字层、智能对象。
5. 对精确文字、价格、参数、按钮文案，默认要求只做占位，不烤进图片；后期在 PSD 里重新排。

推荐规则块：

```text
PSD/分层拆分规则：
- 这张图按后期 PSD 分层逻辑设计：背景、主体、装饰、图标、文字占位、阴影高光、纹理各自清楚分区。
- 每个素材边缘清晰、无遮挡、少重叠，便于后期抠图、分拆成透明 PNG 或重绘为矢量。
- 文字、价格、参数和按钮文案只保留为空白占位区域，不生成正式文字。
- 阴影和高光不要和主体粘死，保持可拆分、可单独调整的视觉关系。
```

需要单独生成素材包时，把提示词拆成多张图：`完整效果预览图`、`背景空版图`、`主体透明 PNG 图`、`图标/装饰平铺图`、`文字占位版式图`。

## 图片编辑规则

编辑任务用“只改变 X，保留 Y”的句式。每次迭代都重新写保留项，避免漂移。

必须保留的内容按需列出：身份、几何形状、品牌元素、文字、构图、相机角度、周围物体、光照方向、阴影、色彩层级、箭头/标签/布局。

产品抠图和商品图默认要求：清晰边缘、无光晕、标签可读、几何不变。需要透明底时，`gpt-image-2` 可直接生成透明 PNG/WebP：明确隔离主体、无场景/实底/棋盘格/粘连阴影，并在后续处理保留 alpha 通道；只有透明边缘不可靠时，再建议白底图与下游去背作为备选。

## 文字入图规则

图片内文字越短越稳。确实需要文字时：

- 把精确文案放进引号，标注 `EXACT, verbatim, no extra characters`。
- 指定位置、层级、字体气质、字号关系、颜色和对比度。
- 品牌名、生僻词、大小写敏感词可逐字母拼写。
- 小字、密集信息图、多字体版式默认用 `quality="medium"` 或 `high`。
- 出图后必须检查：是否多字/少字/错字，是否重复出现，是否压到主体。

正式中文长段落、频繁变化的价格表、复杂课程目录和规则文本，优先放在可编辑层。若用户明确要求图像本身就是最终信息图/海报/幻灯片，允许直接放入确认后的文字与数据，但必须写清原文、层级、位置与可读性，并在出图后逐项验收。

## 多图输入规则

引用图片时按索引和作用说明：

- `Image 1`：主产品/人物/原始照片。
- `Image 2`：风格参考/材质参考/构图参考。
- `Image 3`：品牌色/包装/图案参考。

明确说明“把哪张图的什么应用到哪张图”，例如“保留 Image 1 的产品几何和标签，应用 Image 2 的棚拍灯光与背景材质”。

## 常用参考

需要更多模板时读取：

- `references/prompt-templates.md`：常用提示词模板。
- `references/gpt-image-2-notes.md`：官方特性摘要、参数边界和诊断清单。
